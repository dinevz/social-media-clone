[
    create research html/css and basic crud functionality,

    make update global function to count likes, comments, posts, researches.
]


if(time)
    you can like your own post, 
    comment section is ugly,
    profile page lacks functionality,
    messages should be easy, right?
endif